"""Each level's tests load their own starter.py / solution.py by path (see the
_load helper in every test file), so nothing here is needed to make same-named
modules coexist. This file only marks the repo root for pytest."""
